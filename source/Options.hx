package;

//This hx was originally to the Kade Engine styled Options, but now it is just for storing variables since everything is on the same .hx file

class Options
{
	public static var masterVolume:Float = 1;
	public static var controllerScheme:Int;
}